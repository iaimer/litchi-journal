# Handoff

> 给下一位 agent 的最小必要交接信息。只记录当前仍有价值的状态、决策和后续动作，不重复提交记录或完整 diff。

## 当前目标与上下文

`1.7.5+31` 是当前发布版本：在 `1.7.4+30` 的时间轴与操作布局修正基础上，完成快速记录正文优先布局、阶段 7 全局细节收尾和阶段 1-7 复审修正。完整 Flutter 测试 520 项通过，Release APK 已在 `test_36` 安装并启动。项目流程采用 Project Docs Workflow；旧的 `SESSION_LOG.md` 保留为历史档案，不再追加。

当前未发布分支 `feat/icons-lucide-lordicon-20260919` 已完成静态图标统一到 Lucide 1.47.0、本地 Lordicon 单次动画、旧资源清理和关于页署名页尾收敛。复审修正后完整 Flutter 测试 535 项通过，`flutter analyze` 无问题；`FloraIcon` 继承主题图标颜色/尺寸，动画在减少动态效果时不加载 JSON，资源读取失败时回退静态 SVG。官方 `lordicon 1.0.3` 与 `image 4.x` 的 `archive` 依赖不兼容，因此直接使用 `lottie 3.6.1` 播放官方资源，未降级图片依赖。关于页来源文字为 12sp、主文字色 82% 不透明度，链接热区保持 48dp。`test_36` 复验首页/设置图标和关于页署名页尾；当前模拟器首页 API 请求失败，空状态动画由 Widget 测试覆盖。覆盖安装保留了模拟器应用数据，没有写入真实 Vault；分支尚未提交、推送或发布，版本仍为 `1.7.5+31`。

日记 ZIP 与 WebDAV 备份已撤回。用户改为由外部云盘同步整个 Obsidian Vault，因此客户端、服务端、Android 下载通道和相关依赖均不再维护；服务端提供白名单式遗留数据清理脚本，部署新版服务端时先停止旧服务再执行，不能触碰 Vault 或云盘中的现有文件。

撤回分支已通过服务端 37 项测试、TypeScript 构建、Flutter `analyze`、486 项 Flutter 测试和 Release APK 构建；该次验收未连接 PLG110 真机。本轮 `1.7.5+31` Release APK 已在 `test_36` 模拟器安装并启动，PLG110 真机验收仍由用户按发布流程执行。

Mac mini 已在 2026-09-17 拉取 `63fff12` 并完成停服清理：dry-run 仅发现 `backup-state.json` 和应用专用临时目录，随后已按白名单删除；服务端依赖同步移除了 90 个旧包，TypeScript 构建成功并重启 `diary-api`。健康接口返回 `200`，四个旧备份接口均在鉴权后返回 `404`；复核时 `server/data` 与应用专用临时目录均为空/不存在。Vault、云盘、远端 ZIP 和 Android 下载目录未纳入清理范围。

## 已完成工作

- 首页习惯支持乐观打卡、静默保存、失败回滚和成功完成音效。
- 饮水快捷记录使用底部面板，快捷水量支持全局持久化；运动入口和进度条交互已按当前产品决定保留。
- 饮水与运动进度条使用稳定布局，保存期间保持布局占位，避免数值更新时习惯项目间距瞬间变化。
- 首页习惯模块保持既有 Markdown、服务端接口和统计边界。
- 阅读/亲子阅读、学语言和自定义习惯支持 duration 模式；新增 `FocusTimerScreen`、单会话时间戳恢复、手动追加/设为/清零，以及按开始日写回完整分钟。
- 服务端新增 `/api/v1/diary/habit/duration` 与 `days=all` 统计数据；旧的无分钟 checkbox 记录继续计入完成/连续记录，但不虚构时长。
- 当前版本已递增为 `1.7.2+28`；标签管理本机闭环已发布；步数编辑和饮水手动输入统一为底部面板，步数预填并全选当前值，饮水第二行固定为「手动输入 / 清零 / 自定义预设」；Release APK 已按本版本重新构建，未安装真机。
- 当前版本已递增为 `1.7.3+29`；UI 去 AI 化阶段 1 已完成，日期标题、随手记、觉察、小确幸、焦虑、人生教练、明日寄语和影像记录均已完成对应展示调整；Release APK 已按本版本重新构建，未安装真机。
- 当前版本已递增为 `1.7.4+30`；时间轴轨道覆盖每条记录完整高度，随手记、觉察和小确幸的标签与三点操作热区统一对齐，小确幸按条目保留编辑和删除能力；Release APK 已按本版本构建。
- 当前版本已递增为 `1.7.5+31`；快速记录正文优先、阶段 7 细节统一及全阶段复审修正已纳入主线。完整 Flutter 测试 520 项通过，`flutter analyze` 无问题；Release APK 已构建、签名验证并在 `test_36` 启动。
- 日记 ZIP 与 WebDAV 备份已从 Flutter、Android 与服务端完整移除，旧备份接口返回 `404`；数据同步改由外部云盘客户端覆盖整个 Vault，部署时仅清理备份配置、凭据、状态和应用专用临时文件。
- `server/scripts/cleanup-removed-backup-data.sh` 默认仅预览，传入 `--apply` 才会删除三个固定备份数据文件、同名前缀损坏隔离文件和 Node 临时目录中的应用专用备份目录；服务运行、符号链接或路径异常时会拒绝执行。
- 觉察模块已改为开放式文本组：单条无圆点，多条使用绿色圆点列表；展示层隐藏时间、节点和连接线，编辑仍向 `QuickCaptureScreen` 传入原始时间、标签和 `rawLine`。
- 习惯趋势页已替换旧的反馈卡、节奏谱和单习惯 Tab：支持周/月/年周期，周/月可左右切换，顶部仪表盘最多显示 4 项用户指定习惯并等宽填满模块；窄屏四项使用 2×2；年档位不显示具体年份或左右箭头。
- 时长习惯的顶部数值仅显示所选周期累计时长，统一使用中文小时，非零极短记录显示 `<0.1 小时`；饮水、运动和 checkbox 习惯显示最长连续达标天数并使用中文 `天`。年视图热力图固定为最近六个月的 26 列 × 7 行，周/月/年均补齐灰色日期槽位并显示一至日标签。
- 新增只读 `/api/v1/stats/habit?from=YYYY-MM-DD&to=YYYY-MM-DD` 区间快照；服务端读取 Markdown，Flutter 负责周期聚合、自定义习惯名称/别名映射和缓存。
- 审查修复已覆盖 API 地址切换后的趋势服务重绑定与缓存隔离、aliases 变化导致的缓存失效、旧 checkbox 时长记录完成色、自定义名称关键词误判，以及小时数值格式边界。

## 关键决策与原因

- 保存状态不通过行尾加载图标表达；用户操作先得到即时视觉反馈，后台静默提交，只有失败才提示。
- 所有首页 counter 的直接数值输入统一使用底部面板，避免同一模块并存居中对话框和底部面板两种模式。
- 进度条编辑入口本身承担编辑语义，避免重复放置独立文字按钮。
- 文档交接采用持久化 `HANDOFF.md`；`SESSION_LOG.md` 不再作为当前流程的一部分。
- 计时采用 60 分钟循环环形反馈，不引入周期、提醒或番茄钟休息；同一 App 同时只允许一个习惯计时会话。
- 数据同步交由外部云盘客户端覆盖整个 Obsidian Vault；荔枝日记不再提供 ZIP、WebDAV、自动调度或手机导出能力。

## 未完成事项与下一步

- 撤回备份后的主流程已通过服务端与 Flutter 自动化验证；备份接口不再存在，其他日记、历史、标签和习惯接口保持原样。
- APK 产物路径为 `build/app/outputs/flutter-apk/app-release.apk`；本轮只在 `test_36` 安装验收，未安装到 PLG110。Android Release 目前使用 debug 签名，包名为 `com.example.litchi_journal_flutter`；如需应用商店发布，需先设置正式包名和签名密钥。

## 已知问题与阻塞

- 当前没有已知阻塞。

## 建议技能

- `project-docs-workflow`：推送或发布前的项目文档检查。
- `requesting-code-review`：完成较大改动或准备合并前的审查。
