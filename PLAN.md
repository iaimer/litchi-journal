# 荔枝日记开发计划（PLAN）

> **定位**：产品需求、路线图与开发进度的唯一来源文档。
> **工作流**：新想法先记入「10. 新需求 & 新想法」→ 确认后转为任务；版本发布更新 `CHANGELOG.md`；会话结束追加 `SESSION_LOG.md`；功能/结构变更同步 `README.md`。

---

## 1. 产品定位

荔枝日记帮助用户按日期保存、回看和补充个人生活记录，并把内容长期保存在自己的 Obsidian Vault（兼容 Obsidian 的 Markdown）中。

- **形态**：Flutter 原生客户端 + TypeScript/Express API 服务端（`server/`，原 Web 端迁入）。客户端负责 UI、状态、API 调用；服务端负责 Markdown 读写、Obsidian 兼容与数据同步。
- **核心用户**：希望在手机上快速记录生活、安静回看成长，并把数据掌握在自己手中的用户；记录场景包括亲子生活、小确幸、情绪觉察、焦虑时刻、习惯打卡、随手记录与影像。
- **成功标准**：用户能快速完成记录，清楚理解数据状态，长期信任 App 与 Markdown 数据源的一致性。
- **品牌气质**：温暖、治愈、专注 —— 一本陪伴成长的私人手账，安静不冷淡，亲切不过度可爱。

**反参考**：不做冷峻办公化 / 仪表盘式工具；不用装饰性玻璃、强渐变、复杂动效；不照搬 Web 页面结构；不复刻其他日记 App。

**设计原则**：记录优先（高频入口清楚）；数据可信（App 与 Markdown 始终一致）；回顾自然（日期导航、时间轴、历史内容不制造管理负担）；温暖克制；原生熟悉（遵循 Flutter 与移动端常见交互）。

**无障碍**：浅色/深色模式清晰；关键状态不只依赖颜色（同时用形状/图标/文字）；主要点击区域 ≥48dp，支持字体放大；动效轻量并尊重「减少动态效果」。

## 2. 领域术语

- **历史补录**：为指定的过去日期补充相片、随手记、觉察或小确幸；目标日期没有日记时可创建标准日记，历史已有内容保持只读，不包含焦虑时刻、习惯或 AI 生成内容。_Avoid_：历史编辑、补写日记。
- **有记录日期**：至少包含一条真实日记内容或一张相片的日期；只有空白模板的日期不算。_Avoid_：有文件日期。

## 3. 问题陈述与解决方案

**问题**：现有工具记录生活有摩擦——碎片化便签不可持续、不可结构化；复杂笔记软件记录成本高；多数日记 App 数据封闭在云端；原 Web 版在手机上记录与回看不顺手。

**解决方案**：一款 Flutter 原生日记 App（配套轻量 API 服务端），围绕「记录 → 沉淀 → 回看 → 补录」组织体验：

- **记录优先**：右下角 FAB 扇形菜单一键进入随手记、觉察、小确幸、焦虑四问与图片上传；AI 润色与自动标签降低记录成本。
- **数据可信**：服务端是 Markdown 写入权威，App 展示与 Obsidian 文件始终一致；跨日自动建日记、时间条目自动排序。
- **回顾自然**：今日页时间轴 + 过往照片画廊 / 月历导航 / 历史详情；可安全补录过去日期的文字与相片。
- **长期沉淀**：习惯打卡、标签体系、影像记录随日期落入 Markdown，统计在客户端解释历史数据。
- **温暖克制**：品牌色、留白、Flora 图标与友好文案营造陪伴感。

## 4. 用户故事

### 今日页与快速记录
1. 打开 App 直接看到今天内容，快速进入记录或阅读。
2. FAB 扇形菜单一键选择记录类型（随手记/觉察/小确幸/焦虑四问/图片），2 步内完成记录。
3. 记录时获得 AI 润色与自动标签。
4. 跨过午夜后首次打开自动获得新一天日记，无需手动新建。

### 随手记 / 觉察 / 小确幸
5. 新增一条随手记、觉察或小确幸，捕捉当下的想法与情绪。
6. 对新条目 AI 润色并自动生成标签。
7. 编辑或删除已有条目。
8. 修改条目的发生时间，把记晚的条目归到正确时间点。
9. 新增或编辑时间条目后，时间轴按 `HH:mm` 升序展示。
10. 记录中断时获得草稿保护（2 分钟 TTL），短时离开不丢输入。

### 焦虑四问
11. 通过独立问答页完成焦虑四问，按引导梳理情绪。
12. 对当前回答逐问润色（纯文本、不产生标签）。
13. 回退到上一步继续修改。
14. 同一天焦虑记录唯一且可替换，不被重复堆积。

### AI 能力
15. 在 AI 设置页配置服务（启用、模型、地址、Key、提示词）。
16. 普通入口润色自动返回标签；焦虑润色不返回标签。
17. 一键生成当天人生教练反馈并可重新生成。
18. AI 请求有超时保护，错误提示不暴露 Key。
19. AI 设置提供连接测试，保存前可验证 Base URL、API Key、Model 与实际响应。

### 习惯追踪
19. 对内置习惯（饮水、步数、阅读、语言、补充剂）打卡。
20. 新增自定义打卡习惯、重命名、换图标与颜色。
21. 归档不再使用的习惯；重新启用后历史数据仍可找回。
22. 习惯统计页查看 7 天节奏谱、30 天热力图与反馈卡。
23. 自定义习惯改名后仍能统计历史打卡（aliases 匹配）。
24. 习惯打卡直接写入 Markdown 且保持干净可读。

### 标签体系
25. 记录时通过 TagPicker 选择领域/主题/方法三级标签。
26. 标签设置页重命名、隐藏/启用标签；AI 润色只推荐可见标签。
27. 远程标签配置不可用时仍有默认标签兜底，快速记录永远可用。
28. 编辑旧记录时保留隐藏标签，不破坏历史内容。

### 影像记录
29. 从手机选择图片上传到当天日记。
30. 图片自动压缩到可接受大小，上传稳定、附件不臃肿。
31. 今日页查看、预览大图并删除图片。
32. 一次最多选择 9 张图片批量补录。

### 过往回看与历史补录
33. 过往页以按月照片图墙回看历史，每个有照片日期展示首图，并可随机打开当天全部照片。
34. 月历选择任意过去日期，有真实记录或相片的日期显示圆点。
35. 历史详情只读浏览（隐藏明日寄语与习惯）。
36. 在历史详情页为过去日期补录随手记、觉察、小确幸或相片。
37. 浏览或打开无日记日期不产生空 Markdown 文件。
38. 补录失败时保留已成功上传的图片并明确提示。

### 设置与连接
39. 设置页修改远程服务器地址，复用当前 Token 测试连接后保存。
40. 远程地址保存后立即生效，不需要重启 App。
41. 了解 Token 是否已配置（不展示真实值）。
42. 启动配置、今日日记与标签配置加载有超时兜底，永不无限 loading。

### 视觉与无障碍
43. 统一的暖色手账风格、Flora 图标与品牌视觉（启动页、App 图标、关于页）。
44. 浅色/深色模式都清晰可读，关键状态不只依赖颜色。

## 5. 实现决策

### 架构原则
- **不要把 Markdown 当作 UI**：数据流 `Markdown → Parser → 领域模型 → Flutter 原生组件`；解析集中在 Parser 层，页面不散落 Markdown 语法判断，旧格式兼容也放 Parser。
- **服务端是 Markdown 写入权威**：Flutter 不直接读写 Obsidian 文件、不在本地拼日记模板；服务端负责创建新日记，客户端只发送操作意图。
- **组件命名表达产品语义**（`HabitCard`、`QuickNoteTimeline`、`AnxietyComposer`、`GenericSectionCard`），不表达 Markdown 语法。

### 数据完整性规则（写入类改动必须遵守）
- **rawLine 不可反推**：编辑/删除必须使用 Parser 解析的原始 rawLine，不能用 content + tags 重新组装。
- **时间轴写入即排序**：新增或编辑带 `HH:mm` 的时间条目后，服务端仅在当前 section 内按时间升序重排并直接写入 Markdown，完整保留 `-`/`>` 前缀、正文和标签；不能只在 Flutter UI 排序。
- **tags 前缀差异**：`TimelineContent.tags` 存带 `#` 前缀，TagPicker 与选中态存不带 `#`；编辑弹窗初始化须 strip `#`。
- **`###` 独立 section**：觉察/人生教练/荔枝喵说/明日寄语/影像 为独立 DiarySection，不嵌套为子内容。
- **跨日自动创建**：`_loadDiary()` 中 `getDiary(date)` 返回 null 时先 `ensureDiary(date)` 再重读；提交首次失败须 ensureDiary 并重试。
- **历史补录延迟创建**：浏览/打开无日记历史日期不创建文件；只有文字或相片真正保存时才 `ensureDiary(date)`，所有写入使用用户选择的目标日期。
- **历史相片批量上传**：一次最多 9 张，按选择顺序逐张压缩上传；失败停止后续，保留已成功图片并报告成功数量。
- **草稿 TTL 2 分钟**：草稿是短时保护，不是长期草稿箱，过期自动清除。
- **历史内容只读**：历史日记不开放编辑/删除；补录仅支持随手记、觉察、小确幸与相片（不含焦虑、习惯、AI 生成）。
- **历史画廊只读**：画廊接口按月份倒序分页，只读取已有日记；纯文字日期仍从月历进入，浏览不创建文件、不修改 Markdown。
- **历史画廊图片**：每个日期只展示首图，沉浸预览按当天 Markdown 顺序加载全部图片；渲染接口不向 Vault 写缩略图缓存，客户端缩略图缓存最多保留 60 张。

### 习惯 / 标签 / AI 边界
- **习惯**：App 决定习惯定义（名称、图标、颜色、启用/归档），服务端只负责打卡写入，Markdown 只保存完成状态（如 `- [x] 📝 冥想`）；自定义习惯内部 key（`custom_<时间戳>`）不写入 Markdown；改名后靠 `customHabitAliases` 匹配历史；统计完全在客户端完成，服务端不聚合。
- **标签**：三级模型（领域/主题/方法），用既有 `id` 作稳定 key；远程优先、`DefaultTagConfig.value` 兜底，标签设置入口不能静默失败；正文标签 chip 在模块上下文中跟随模块色，TagPicker 领域按固定色板、主题统一 `#F2C94C`、方法统一 `#9775FA`。
- **AI 分场景**：普通入口（quickNote/reflection/happiness）走 `polish()` 返回内容+标签；焦虑走 `polishPlainText()` 不含标签；人生教练是回顾分析链路，不混入普通输入润色。

### 快速记录入口
- 今天页只保留内容展示与右下角 FAB，不放首页内联输入区；FAB 子入口只负责路由，不重写保存逻辑；扇形菜单用极坐标定位。
- 随手记/觉察/小确幸统一进 `QuickCaptureScreen`；焦虑四问进 `AnxietyScreen`；图片入口直接调用现有选择/压缩/上传/刷新流程。
- 过往页本身不显示补录 FAB；进入某天历史详情后才显示历史补录入口。

### 网络与安全
- 服务端认证只接受严格 `Token <value>` 格式 + 恒定时间比较；不做宽松替换解析。
- CORS 支持 `allowedOrigins` 白名单；空白名单表示保持本地调试与真机访问兼容。
- API 请求 12 秒超时、图片上传 30 秒超时；图片压缩后仍超限时请求前明确提示；上传失败区分超时/认证/图片过大/服务器错误。
- 远程 API 地址修改须用当前 Token 测试新地址，成功后才保存并立即替换 `ApiClient`，不要求重启。
- 错误信息不含磁盘路径；不记录、不输出 Token、API Key 或敏感配置。

### 品牌与视觉
- `docs/design-reference/` 是品牌视觉权威源图；启动页、App 图标、关于页品牌图必须从源图派生，不重新绘制近似图。
- Today Rainbow 模块色序固定：随手记红 `#FF6B6B`、小确幸橙 `#FF9F43`、焦虑黄 `#FFD43B`、觉察绿 `#51CF66`、人生教练青 `#12B5CB`、明日寄语蓝 `#4DABF7`、影像记录紫 `#9775FA`；模块色/标签色不写入 Markdown。
- 习惯图标统一走 `HabitIcon`：新配置渲染 SVG，旧 emoji 配置继续兼容；禁止直接 `Text(icon)` 展示逻辑名。
- 真机覆盖安装用 `adb install -r` 保留本地 baseUrl 与 Token。

## 6. 测试决策

- **原则**：只测外部行为，不测实现细节；写入类改动先复现再修复。
- **质量基线**：`flutter analyze` 零问题；`flutter test` 全绿（当前 355 项）；`server npm run build` 通过；`server npm test` 全绿（当前 29 项，测试脚本限定 `src`，避免收集 `dist` 产物）。
- **重点模块**：服务端 Markdown 解析与 section 内时间排序、YAML frontmatter 列表解析、编辑/删除 rawLine 匹配、焦虑 replace 严格校验、图片格式校验、auth 恒定时间比较；客户端 widget 测试覆盖今日页/快速记录/焦虑/习惯/标签/过往/补录/远程 API 核心链路。
- **真机验收**：涉及视觉体验用真机（PLG110，Android 16，无线 ADB）截图验收；覆盖安装固定 `adb install -r`。
- **环境限制**：沙箱内 `flutter test` 偶发无法创建本地 socket、`adb` smartsocket "Operation not permitted" 属环境限制，不视为代码缺陷。

## 7. 范围外（Out of Scope）

- **Open Design 全局 UI 重设计**：功能稳定后统一推进；当前以 `DESIGN.md` 为设计语言基线。
- **动态习惯**：服务端 `/habit` 仍为固定字段，自定义计数习惯、目标/单位/周期/提醒不支持（需服务端先支持）。
- **标签体系完整管理**：当前为 MVP（重命名、启用/隐藏、恢复默认、新增领域/主题/方法）；排序、删除后兼容、服务端同步与冲突处理未做。
- **历史内容编辑/删除**：保持只读。
- **月末/年末习惯回顾**：仅模板草案与方案讨论，未实现。
- **云同步/多设备/Web 版**：不在 Flutter 客户端职责内。
- **Flutter 本地拼日记模板**：明确禁止，由服务端 `create` 负责。

## 8. 开发进度

### 8.1 版本线（截至 2026-08-29，1.6.0+19）

| 版本 | 主题 | 关键内容 |
|---|---|---|
| 1.0.0 | 基础能力版 | 今日页记录、AI 润色与自动标签、习惯追踪、过往页、习惯统计、设置、图片、人生教练、底部导航 |
| 1.1.0 | 记录体验版 | 外观设置、习惯/标签设置、FAB 扇形快速记录入口、统一记录页/焦虑独立页、品牌资源 |
| 1.2.0 | 骨架统一版 | `FloraPageScaffold`、Android 12+ 启动屏、卡片样式一致 |
| 1.2.1 | 细节修复 | FAB 图标、下拉刷新背景、饮水自定义毫升、关于页 MarkdownBody |
| 1.3.0 | 自定义习惯 H1 | 自定义普通打卡习惯完整闭环（设置/今日页/Markdown 写入） |
| 1.4.0 | 习惯管理 H2 | 启用/归档分区、同名校验、customHabitAliases 进入统计 |
| 1.4.1–1.4.2 | 视觉细化 | 颜色候选池扩展、标签管理页重构、Today Rainbow Phase 1-2 |
| 1.4.3 | Today Rainbow 校准 | 模块色序固定、正文标签随模块色、TagPicker 独立标签色规则 |
| 1.4.4 | 连接与统计体验 | 热力图 icon-only Tab、远程 API 地址编辑（先测后存） |
| 1.4.5 | 上线前加固 | API/AI 超时、错误文案去敏感信息、严格 `Token <value>`、CORS 白名单 |
| 1.4.6 | 时间编辑 | 条目发生时间编辑 + 服务端 section 内 `HH:mm` 排序 |
| 1.5.0 | 历史回看与补录 | 过往月历、有记录圆点、历史详情补录 FAB、文字+最多 9 张相片补录、延迟创建日记 |
| 1.5.1 | 服务端加固 | 删除重复/死路由、图片格式与大小校验、habit/stats 输入校验、YAML 列表解析修复、死代码清理 |
| 1.6.0 | 过往画廊 | 按月照片图墙、当天沉浸式多图预览、月份分页与跳转、渲染图片接口；保留月历、只读详情与历史补录 |

### 8.2 里程碑概述（Sprint 1-40）

- **Sprint 1-7**：工程初始化 → 结构化 Markdown 解析 → 设计系统与 section 化 UI → 领域模型 → 标签三级模型 → 输入体验 → 多入口快速记录与焦虑四问。
- **Sprint 7F-10C**：草稿保护（2 分钟 TTL）→ 习惯打卡闭环 → AI 润色与自动标签 → 焦虑单日唯一与编辑替换 → 条目编辑/删除/觉察独立 section → 跨日自动建日记。
- **Sprint 11-13**：图片上传/压缩/预览/删除；一键人生教练；过往页 MVP 与只读详情。
- **Sprint 14-17**：顶部 UI 修复与 `adb install -r` 覆盖安装流程；习惯统计页（热力图/节奏谱/缓存）；导航收敛 3 Tab；习惯归档刷新链路。
- **Sprint 28-33**：标签设置 MVP；FAB 快速记录 V2/V3；启动/标签/习惯图标稳定性兜底；品牌视觉源图落地；页面骨架统一；Android 启动屏修复。
- **Sprint 34-36**：自定义习惯 H1（1.3.0）；习惯管理 H2 与统计 aliases（1.4.0）；Today Rainbow 模块色校准（1.4.3）。
- **Sprint 37-39**：条目时间编辑与 Markdown 排序（1.4.6）；历史月历与历史补录（1.5.0）；全面审查与服务端加固（1.5.1）。
- **Sprint 40**：过往画廊 v1（1.6.0）：照片优先图墙、月份分页、当天沉浸式预览与图片渲染链路。

### 8.3 当前状态结论

当前版本定位为「Flutter 客户端 + 本仓库 API 服务端」的完整日常记录与回顾版，已具备独立日常使用能力：文字记录闭环、AI 润色与自动标签、习惯追踪与统计、标签体系、图片链路、按月照片画廊与当天沉浸预览、月历和历史补录、远程 API 配置、品牌视觉与稳定性兜底均已落地。

## 9. 路线图与未完成清单

### 9.1 近期方向

1. **稳定观察**：真实使用 1-2 天，验证历史补录数据完整性与多图上传体验，记录摩擦点；不建议立刻新增大功能。
2. **Bugfix Sprint**：把真实使用中的小问题统一修复，形成稳定版。
3. **画廊稳定观察**：真实使用 1-2 天，重点验证连续翻阅、图片失败重试、内存占用与深色模式。
4. **Open Design 全局 UI 重设计**：功能稳定后统一推进。

### 9.2 未完成 / 可规划

- [ ] Open Design 全局 UI 重设计：形成适合 Flutter 原生体验的视觉与交互语言。
- [ ] 服务端统一人生教练生成 API（当前 Flutter 端仍为本地生成/解析链路）。
- [ ] 动态习惯（需服务端先支持）。
- [ ] 月末/年末习惯回顾（模板草案已就绪，写入位置与时机待定）。
- [ ] 标签体系完整管理（增删改、排序、同步服务端、冲突处理）。
- [ ] 完整日历式历史页（当前月历已覆盖主要需求）。

### 9.3 不建议做

- 继续扩大 Flutter 端人生教练自由文本正则解析。
- 在 Flutter 本地拼日记模板。
- 把标签配置完全依赖远程接口而移除默认兜底。
- 把旧 emoji 习惯配置强制迁移为 SVG 图标（避免丢失用户自定义设置）。
- 功能未稳定时重做全局 UI / 一次性重构所有组件。

## 10. 新需求 & 新想法

> 📥 未处理的灵感和需求，确认后转为正式任务。有新想法时立即在此追加。

- [x] 将低使用率的「过往」页改造为照片驱动的画廊式回顾：以按月分组的图墙为默认视图，保留日期定位、历史只读详情与补录能力；参考片羽集的视觉回顾思路，但延续荔枝日记「照片进入完整日记故事」的产品定位（2026-08-29，已在 1.6.0 实现）
