# 荔枝日记

荔枝日记当前仓库包含 Flutter 原生客户端和 API 服务端。

Flutter 客户端负责 UI、状态管理和 API 调用；`server/` 负责 Markdown 读写、Obsidian Vault 兼容和数据同步。原 Web 端服务端已经迁入本仓库，后续服务端开发以这里为准。

## 当前状态

当前版本：`1.7.6+32`

`1.7.6+32` 修复随手记、觉察和小确幸的多自然段展示与 Markdown 数据链路。标签和三点菜单统一位于末段下方；Parser 保留完整 `rawLine`，服务端写入稳定续行前缀，排序只移动完整记录块，编辑/删除要求多行 `rawLine` 精确匹配。完整 `flutter test` 541 项、服务端 52 项测试、`flutter analyze` 与 TypeScript 构建均通过。Release APK 已生成于 `build/app/outputs/flutter-apk/app-release.apk`，APK 版本信息和签名校验通过；真机安装与验收由用户执行。

此前图标迁移已在 `test_36` 对首页、过往、快速记录、设置和关于页做代表性检查；复审后再次验证首页/设置图标颜色及关于页署名页尾。关于页页尾在浅色、深色、约 320dp 窄屏与 1.3 倍字体下均无溢出；动画空状态与加载失败回退由 Widget 测试覆盖。当前多自然段修复没有重新安装 APK，也未修改真实 Vault。

UI 去 AI 化阶段 1 第二批已实现：焦虑改为共享页面底色的开放式问答组，人生教练去除内层色块、圆角和长左线，明日寄语保持无外框阅读区，影像记录使用两列方形图网格；已在 `test_36` 模拟器完成浅色、深色、1.3 倍字体与约 320dp 窄屏验收。

UI 去 AI 化阶段 2、3 已实现：快速记录显示「润色」，日记展示统一使用「今日回顾」，记录页错误文案不透出技术细节；系统章节标题在展示层去除 emoji，用户自定义章节标题保持原样。自动化与 `test_36` 模拟器验收均已完成，验收后已恢复模拟器默认外观。

阶段 4、5 已实现：所有 App 主动指定的静态图标统一使用 Lucide 1.47.0；过往空状态与正向 SnackBar 使用本地 Lordicon 单次动画，支持减少动态效果回退并在关于页署名。Flutter 原生控件内部图形和荔枝品牌资源保持不变。浅色和深色主题补齐 Material 3 语义色，标签按最终混合背景计算对比度，Callout 改为主题感知颜色。

阶段 6 已实现：快速记录把时间收敛为轻量元信息，把正文编辑区放到页面主体；已选标签显示在正文下方，标签面板改为页面内渐进展开，今日新建自动聚焦、编辑和历史补录不主动弹出键盘。保存、润色、标签、草稿和编辑写入行为保持不变，新增布局测试覆盖窄屏与放大字体。

阶段 7 已实现：统一一级标题、二级 AppBar、圆角和动效令牌；首页、过往、习惯趋势、历史详情及设置相关页面首次加载使用静态内容占位，刷新保留已有内容；空状态和错误状态避免重复文案并提供重试；深色启动背景与页面纸色一致，系统开启减少动态效果时不等待淡入。已通过阶段 7 定向测试、完整 Flutter 回归、`flutter analyze` 和 `test_36` 模拟器验收。

当前版本定位：

```text
Flutter 客户端 + 本仓库 API 服务端版
```

截至 2026-09-23（1.7.6），已完成：

- 今日页日记读取与结构化展示
- 随手记、觉察、小确幸、焦虑四问写入
- 快速记录 FAB：右下角扇形入口进入随手记、觉察、小确幸和焦虑四问；图片不再是独立 FAB 项
- 统一记录页：随手记、觉察、小确幸的新增与编辑复用同一个二级记录页，焦虑四问使用独立问答页
- 图文记录：随手记与小确幸可在新增、编辑和历史补录时添加最多 9 张照片；觉察不支持照片
- AI 润色与自动标签
- 阶段 6 快速记录布局：正文优先、轻量时间元信息、页面内展开标签和稳定底部保存
- 阶段 7 全局细节收尾：标题、圆角、加载反馈、空状态文案、启动背景和减少动态效果规则统一
- AI 服务配置支持请求级连接测试
- 条目编辑、删除与发生时间修改；操作入口使用底部操作面板；多自然段正文保持完整记录块，标签和操作菜单位于末段下方；新增或编辑后，当前 Markdown section 内的时间条目自动按 `HH:mm` 排序
- 习惯追踪交互：饮水、步数进度条与每日目标设置；点击饮水进度条可快捷添加、手动输入或编辑三个常用水量，点击步数进度条打开同风格的底部输入面板；运动使用成长绿色
- 专注计时：阅读/亲子阅读、学语言和自定义习惯可切换为时长模式；支持单会话暂停/恢复、后台时间戳恢复、手动追加/设为/清零，按开始日期写回分钟，并在统计页展示累计与平均时长；首页习惯模块使用状态圈、图标、名称、真实进度条和当前/目标数值的紧凑单行布局
- 习惯默认图标与候选图标 SVG 化，兼容旧 emoji 配置
- 标签设置页支持名称直达底部操作菜单、统一底部表单、新增/重命名/停用/永久删除/恢复默认；本机标签会同步影响 TagPicker 与 AI，远程标签配置不可用时使用 Flutter 本地默认兜底
- 图片上传、压缩、显示、预览、删除
- 图文记录图片选择后立即显示本地预览，按图片显示待上传、预处理、真实上传进度、成功和失败状态；正文下方使用固定 3 列方形网格，即使只有 1–2 张也不拉伸铺满
- 图文记录图片在 Markdown 中仍保存于「影像记录」，并以隐藏 UUID 标记关联到对应随手记或小确幸；页面展示于正文末段下方、标签及操作菜单上方，影像模块不重复显示有效关联照片
- 编辑页对已保存照片的移除暂存到点击保存时提交；删除图文记录会精确移除关联链接，仅在附件不再被引用时删除文件。旧式独立图片继续保留在影像记录和过往画廊中
- 今日回顾一键生成，并兼容旧格式中的人生教练与荔枝喵说标题
- 明日寄语展示
- 过往页按月照片画廊：三列图墙、月份统计、连续分页、月份跳转和随机回顾
- 当天沉浸式多图预览：支持左右滑动、双指缩放、失败重试和从底部进入当天只读日记
- 过往月历：按月选择指定历史日期，有真实记录或相片的日期显示圆点
- 过往只读详情；已有历史内容不开放编辑或删除
- 历史补录：支持为指定过去日期补录随手记、觉察、小确幸；随手记和小确幸可在记录页内附加最多 9 张相片
- 无日记历史日期仅在首次真正保存时创建标准日记，避免误触产生空文件
- 过往详情隐藏明日寄语和习惯追踪
- 过往页固定 header，不随内容滚动；画廊浏览不创建日记或修改 Markdown
- 画廊索引过滤 Vault 中缺失附件；缺失图片显示为不可用，网络或服务错误保留重试入口
- Android release 版远程服务端连接
- 远程 API 配置编辑：支持在设置页修改服务器地址，并复用当前 Token 测试连接后保存
- 远程 API 地址保存后即时生效，不再需要重启 App
- 启动配置读取、今日日记加载和标签配置加载的超时/兜底保护
- API / AI 请求超时保护与可操作错误提示，图片上传失败会显示具体原因
- DeepSeek 官方 V4 按目标 Host 使用 non-thinking 请求；AI 服务不可用时显示明确错误
- 品牌视觉资源落地：启动页、App 图标、关于页品牌图均使用 `docs/design-reference/` 中的原始参考图派生
- 全应用图标统一：Lucide 1.47.0 负责静态图标，Lordicon FREE 书本与勾选图标用于空状态和成功反馈；关于页署名已加入，正式发布说明必须包含 `Animated icons by Lordicon.com`
- Today Rainbow 模块视觉：随手记红、小确幸橙、焦虑黄、觉察绿、今日回顾青（兼容旧称人生教练）、明日寄语蓝、影像记录紫
- UI 去 AI 化阶段 1：日期标题两级化，随手记保留无外框时间线，觉察与小确幸使用开放式文本组，焦虑与人生教练共享页面底色，明日寄语无背景阅读区，影像记录使用两列方形网格
- UI 去 AI 化阶段 2、3：快速记录使用「润色」，今日页将人生教练和荔枝喵说统一显示为「今日回顾」，记录错误文案保持功能导向；系统章节标题仅在展示层规范化，`generic` 自定义标题和 Markdown 原文保持不变
- 时间轴与记录操作布局修正：随手记轨道连续覆盖每条记录，随手记、觉察和小确幸的标签与三点菜单统一对齐，小确幸保留逐条编辑和删除能力
- 日记正文标签 chip 跟随所在模块色，快速记录页 TagPicker 使用领域不同、主题统一、方法统一的稳定标签色规则
- 习惯趋势页重构：周/月/年切换、周/月日期导航、最多四项可配置的等宽仪表盘和逐习惯点阵热力图；窄屏四项仪表盘使用 2×2，仪表盘按当前自然年聚合，年档位热力图只展示最近六个月的 26 列 × 7 行，周/月/年统一补齐灰色日期槽位并标注一至日；时长统一以中文小时展示，非零极短记录显示 `<0.1 小时`，达标连续天数以 `天` 展示；缓存按远程 API 隔离，跨日会刷新统计，旧 checkbox、自定义名称、大字体和 365/366 天边界已通过回归验证

## 关键文档

推送工作流使用以下 5 份核心项目文档：

- `AGENTS.md` — 项目知识库：规则、架构、数据完整性与开发约束。
- `HANDOFF.md` — 当前可交接状态、关键决策、未完成事项与已知阻塞。
- `CHANGELOG.md` — 已发布版本的变更记录。
- `README.md` — 项目简介、当前状态、验证命令与发布流程。
- `PLAN.md` — 产品需求、实现决策、路线图与开发进度。

补充文档：

- `DESIGN.md` — 设计语言：色彩、排版、组件、Flora 图标与空状态规范。
- `docs/THIRD_PARTY_NOTICES.md` — Lucide 与 Lordicon 静态/动画图标来源及许可说明。
- `UI_REFINEMENT_PLAN.md` — UI 去 AI 化改进计划、阶段决策与验收标准。
- `PRODUCT.md` — 产品用户、定位、品牌性格与设计原则。
- `SESSION_LOG.md` — 旧流程遗留的历史档案，不再更新，也不作为接手必读文档。
- `docs/design-reference/` — 品牌视觉源图，更新启动页或 App 图标时优先读取这里。

新 agent 接手前应先阅读 `AGENTS.md`、`HANDOFF.md` 和 `PLAN.md`，再按需查阅 `README.md`、`DESIGN.md` 与 `CHANGELOG.md`。

## 验证命令

```bash
/Users/yezi/development/flutter/bin/cache/dart-sdk/bin/dart analyze lib test
/Users/yezi/development/flutter/bin/flutter analyze
/Users/yezi/development/flutter/bin/flutter test

cd server
npm run build
npm test
```

构建并安装 release 包：

```bash
/Users/yezi/development/flutter/bin/flutter build apk --release
adb -s <device-id> install -r build/app/outputs/flutter-apk/app-release.apk
```

注意：真机验证前必须先构建对应模式。日常覆盖安装要用 `adb install -r`，在同 packageId、同签名、非降级安装时会保留本地 baseUrl/token。不要用 `flutter install --release` 做日常覆盖安装，因为它可能先卸载旧版，导致 token 丢失。

## 发布检查清单

发布前请确认：

- [ ] 更新 `pubspec.yaml` 中的 `version`
- [ ] 更新 `CHANGELOG.md` 对应版本内容
- [ ] `dart analyze` 无问题
- [ ] `flutter test` 全部通过
- [ ] `flutter build apk --release` 构建成功
- [ ] 真机安装验证核心功能
- [ ] 关于页显示正确版本号与当前版本更新内容
- [ ] 外观主题设置：三种模式均正常工作，深色模式主要页面可读
- [ ] 标签设置、AI 润色正常

当前 `android/app/build.gradle.kts` 的 Release 构建仍使用 debug 签名，`applicationId` 为 `com.example.litchi_journal_flutter`。生成的 APK 是 Release 模式内部构建；上架应用商店前需配置正式包名和签名密钥。

## 后续方向

短期优先验证习惯趋势页在周/月/年周期、浅色/深色模式和窄屏下的可读性，再观察画廊连续浏览的流畅度、图片失败重试、内存占用，以及历史补录的数据完整性和多图上传体验。

可规划但尚未完成：

- Open Design 全局 UI 重设计

## 服务端

服务端位于 `server/`，是从原 Web 项目迁入的 TypeScript/Express API。

本地配置不会提交到 Git。第一次运行前复制模板：

```bash
cd server
cp config.example.json config.json
```

然后编辑 `config.json`：

```json
{
  "vaultPath": "/path/to/your/Obsidian Vault",
  "apiToken": "<YOUR_PRIVATE_TOKEN>",
  "port": 4001,
  "allowedOrigins": []
}
```

`allowedOrigins` 为空数组时保持当前本地调试和真机访问兼容；需要限制浏览器跨域来源时填写允许的 Origin 列表。原生 Flutter 客户端不依赖浏览器 CORS。

常用命令：

```bash
cd server
npm install
npm run build
npm test
npm run dev
```

Mac mini 部署：

```bash
cd server
./deploy.sh
```

服务端运行要求 Node.js `20.9+`（`sharp` 依赖要求）；更新已有部署时先执行 `npm install` 再构建。

健康检查：

```bash
curl http://localhost:4001/health
```

图片上传接口已支持可选 `imagePrefix`。旧客户端不传时继续生成 `Image-YYYYMMDD-NNN.jpg`；Flutter 新客户端传入合法前缀时会生成 `{prefix}-YYYYMMDD-NNN.jpg`。

画廊使用 `GET /api/v1/history/gallery?cursor=YYYY-MM&limit=3` 按月份分页；服务端只索引 Vault 中实际存在的安全图片文件，避免 Markdown 与附件尚未同步时产生必然 404。图片预览使用鉴权的 `GET /api/v1/diary/image/render/:year/:imageName?month=MM&maxWidth=...`，服务端按需返回经过方向纠正和缩放的二进制图片，不在 Vault 写缩略图缓存。

习惯趋势页使用只读的 `GET /api/v1/stats/habit?from=YYYY-MM-DD&to=YYYY-MM-DD` 获取最多 366 天的每日快照；服务端只解析已有 Markdown，不会因为浏览统计创建日记或修改文件。
