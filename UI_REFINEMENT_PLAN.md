# 荔枝日记 UI 去 AI 化改进计划

> 状态：阶段 1 第二批、焦虑与人生教练开放式布局、阶段 2、3 去 AI 化展示、阶段 4、5 图标与颜色系统、阶段 6 快速记录正文优先布局，以及阶段 7 全局细节收尾均已实施；自动化与 `test_36` 模拟器验收均已完成。
>
> 设计模式：保留品牌、定向演进。
>
> 当前进度：阶段 1-7 已完成；焦虑、今日回顾、明日寄语和影像记录已按内容语义脱离统一卡片结构，图标职责、主题语义色和对比度检查已落地，快速记录已调整为正文优先布局，全局标题、圆角、加载、空状态和启动过渡规则已统一。

## 1. 目标

这轮改进不追求换一套流行视觉，也不把荔枝日记做成营销页、效率工具或数据仪表盘。目标是在保留暖纸色、Today Rainbow、Flora 品牌资产和原生交互的前提下，减少界面中容易显得模板化、拼装化和刻意强调 AI 的部分，让日期、文字、习惯与相片重新成为视觉主角。

设计判断：这是面向个人长期使用的原生日记产品，气质应当温暖、克制、生活化、低干扰。目标参数为：

- `DESIGN_VARIANCE: 4`：允许少量不对称和内容差异，但不做实验性布局。
- `MOTION_INTENSITY: 3`：只用解释状态变化和空间关系的轻量动效。
- `VISUAL_DENSITY: 5`：保留日常使用效率，同时给正文和相片足够呼吸空间。

## 2. 保留与边界

### 必须保留

- 暖纸色背景、墨棕文字、荔枝陶红主色和深色模式。
- Today Rainbow 的固定模块语义，不把颜色写入 Markdown。
- 今天、过往、习惯三个底部工作区。
- 现有快速记录入口、历史补录边界、焦虑四问和习惯交互逻辑。
- Markdown、Parser、领域模型、Flutter 原生组件的数据流。
- 品牌启动图、App 图标和关于页品牌图的权威源文件。

### 本轮不做

- 不修改服务端 API、Markdown 模板或 Vault 内容。
- 不重做信息架构，不新增功能，不改变已有记录的读写语义。
- 不引入新的 UI 框架、动效库或字体依赖。
- 不一次性重写所有页面，按阶段逐项讨论、实现和模拟器验收；真机验收作为后续发布前确认。

## 3. 当前问题概览

1. 今天页几乎所有领域内容都使用同一种彩色 `SectionCard`，连续出现时像换色后的模板组件。
2. AI 能力在记录页以「AI 润色」直接占据常驻操作，人生教练又使用多组 emoji 标题，AI 身份比用户内容更显眼。
3. Flora 图标、Material 图标和 Markdown emoji 同时出现，部分 Flora 资源的视觉风格与语义也不一致。
4. 浅色模式的主色、次级文字和 muted 文字存在对比度不足；Material 3 的部分语义色没有显式定义。
5. 快速记录页的时间卡片、标签按钮和输入框更像通用表单，正文输入还不是绝对主角。
6. 日期层级、圆角档位、加载状态和常驻抒情文案仍有进一步统一空间。

## 4. 改进阶段

### 阶段 1：重整今天页模块层级

目标：减少连续彩色卡片形成的模板感，同时保证用户仍能快速辨认习惯、随手记、小确幸、焦虑、觉察、人生教练、明日寄语和影像记录。

当前问题来自两个叠加因素：一是所有模块共享相近的圆角、边框、标题条和内边距；二是每张卡又同时使用模块色背景、模块色边框、模块色标题底和模块色标题。单张卡片清楚，整页连续阅读时却会形成重复的「标题条 + 内容框」节奏。

#### 方案 A：轻度去卡片化

- 所有模块继续使用统一容器。
- 去掉彩色标题底，只保留中性纸面、细边框和标题旁的小色标。
- 优点是改动小、回归风险低。
- 缺点是整页仍然是同一种卡片节奏，只是更安静。

#### 方案 B：按内容语义分层，推荐

- 习惯保留紧凑工具卡，因为它包含高频交互和明确边界。
- 随手记与觉察改成无外框时间线，标题位于内容上方，模块色只用于时间、节点或细线。
- 小确幸使用简洁文本组，不为单条内容建立完整卡片外壳。
- 焦虑保留轻量强调面，但不使用与普通模块相同的标题卡结构。
- 人生教练与明日寄语采用安静的阅读区块，通过排版和留白建立层级。
- 影像记录优先让相片占据宽度，外框退居次要位置。
- 优点是每个领域对象拥有与内容匹配的视觉结构，最符合项目现有领域模型方向。
- 缺点是涉及多个现有组件，需要更完整的截图和回归验收。

#### 方案 C：完全手账流

- 除习惯外，所有模块都取消外框，只使用标题、留白和少量分隔线。
- 优点是最接近纸面阅读。
- 缺点是在内容较多、类型交错和深色模式下可能失去扫描边界，当前不建议直接采用。

#### 推荐决策

采用方案 B，但分两步落地：

1. 先处理随手记、觉察、小确幸和人生教练，验证连续阅读节奏。
2. 再处理焦虑、明日寄语和影像记录，让情绪问答、收束文字和图片分别使用匹配语义的展示结构。

#### 实现边界

- 不修改 `MarkdownParser`、领域模型、服务端模板和任何写入接口。
- 不直接全局改变 `SectionCard`，避免同时影响习惯、图片和设置页面。
- 优先在现有语义组件中调整外观，例如 `QuickNoteTimeline`、`ReviewCard`、`AnxietyCard` 和人生教练专用渲染。
- 只有多个语义组件确实共享同一标题结构时，才提取一个小型通用标题组件。
- Today Rainbow 每个模块只选择一至两个主要视觉载体，避免背景、边框、标题底和正文同时染色。

#### 验收标准

- 今天页不再呈现连续七八张同款换色卡片。
- 不看模块色时，仍能通过标题、内容结构和图标辨认主要模块。
- Today Rainbow 映射不变，标签仍遵守模块上下文色规则。
- 习惯交互密度、时间线顺序、编辑删除入口和相片操作保持不变。
- 浅色、深色和系统字体放大后均无文字截断、重叠或层级丢失。
- 320dp 窄屏与 `test_36` 模拟器上完成截图对比。
- `flutter analyze` 与 `flutter test` 全部通过。

#### 已确认的决定

- 采用方案 B 作为阶段 1 的方向。
- 随手记和觉察共用无外框时间线；模块色只保留在时间、节点和标签上下文。
- 人生教练使用中性开放式阅读区：与页面共享底色，通过 Flora 小标题、正文层级和段落留白建立结构，不使用内层纸面、圆角或青色长左线。
- 今天页与历史只读详情共用日期标题结构；今天显示「月日 + 星期」，历史详情额外显示年份。
- 人生教练标题和小标题在展示层去掉 emoji，使用 Flora 图标；Markdown 原文与旧格式解析保持不变。

#### 本次实施记录

- 新增 `DiaryDateTitle`，把日期拆成主日期和星期元信息，并适配系统字体放大。
- 新增 `JournalSection` 与 `JournalTimelineRow`，让标题、色标、时间、节点、标签和操作入口形成稳定的阅读结构。
- 随手记、觉察改为无外框时间线；小确幸改为无外框文本组；习惯卡继续保留高密度工具卡。
- 人生教练改为中性反馈区，并将模式识别、矛盾指出、批判性问题、暖心鼓励和甜点映射为 Flora 图标。
- 时间线行预留右下角快速记录入口的安全区，避免大字号和窄屏下正文被固定 FAB 覆盖。
- 未修改服务端 API、Markdown 模板、Parser、领域模型和记录写入语义。

#### 第二批实施记录

- 焦虑时刻改为 `JournalSection`，四组问答共享页面底色并组成开放式纵向内容组；问题使用黄色小圆点和小号半粗标签，回答与问题正文对齐，不再使用浅黄纸面、内层边框、阴影或独立卡片。
- 焦虑模板没有真实回答时仍显示问题提示；出现真实回答后只显示已回答的问题，保留原有格式过滤和异常内容兜底。
- 明日寄语改为无外框的 `JournalSection` 阅读区，标题统一显示为“明日寄语”，蓝色只保留在章节色标，正文使用稍宽松的行距。
- 影像记录改为 `JournalSection` 加两列方形网格，单图也只占左侧一格；单元尺寸按可用宽度动态计算，已保存图片与上传队列之间保留明确间距。
- 图片顺序、预览、删除菜单、`rawLine`、错误状态和 `selected`、`preparing`、`uploading`、`success`、`failed` 上传状态均保持不变。
- 三个模块的存储标题仍保留 Markdown 原文，展示层只去除 emoji，不修改服务端、Parser、领域模型和公开组件参数。

#### 首批验收记录

- `flutter analyze`：通过。
- `flutter test`：446 项通过（其中 `widget_test.dart` 401 项）。
- `flutter build apk --release`：通过。
- `test_36` 模拟器：浅色、深色、1.3 倍系统字体和约 320dp 窄屏均完成检查。

首批验收记录归档；阶段 1 第二批验收结果见下方。

#### 第二批验收

- 针对性 widget tests：焦虑问答过滤与开放式问答组、人生教练开放式正文、明日寄语纯标题与正文、影像两列方形网格和历史上传重试均通过。
- `flutter analyze`：通过。
- `flutter test`：通过。
- `test_36` 模拟器浅色：焦虑开放式问答组、人生教练无内层容器、明日寄语无背景正文、影像两列网格均通过检查；三张图片顺序正确。
- `test_36` 模拟器深色与 1.3 倍字体：焦虑黄色问题标记、蓝紫色章节色标对比清晰，焦虑问题和回答正常换行，人生教练小标题与正文层级稳定。
- `test_36` 模拟器深色与 1.3 倍字体：焦虑、人生教练和明日寄语均预留 72dp 右侧阅读安全区，正文未被 FAB 遮挡。
- `test_36` 模拟器约 320dp 窄屏：四组焦虑问答、人生教练正文和明日寄语均正常换行且不横向溢出；影像两列保持方形，第三张图片落在左列并可继续滚动到完全可见。
- 上传状态的 `selected`、`preparing`、`uploading`、`success`、`failed` 由现有 widget tests 覆盖，历史失败项重试通过。
- 模拟器使用 `/tmp` 临时 Vault 和测试 API，未修改真实 Vault；验收完成后已恢复模拟器物理尺寸、浅色模式和默认字体比例。

### 阶段 2：弱化 AI 的界面存在感

**状态：已实施。**

- 快速记录按钮统一显示「润色」，焦虑回答保留「润色当前回答」；图标、加载动画、禁用状态和原有回调不变。
- 记录场景错误统一转换为面向用户的功能文案：空内容、未配置、配置错误、超时、网络、限流和服务异常均不透出 API、Model、状态码或上游原文。
- 「人生教练」和「荔枝喵说」在展示层统一为「今日回顾」；生成、更新、空状态、成功和失败反馈使用记录语境，不把 AI 身份放在日记正文旁。
- AI 服务商、Model、API 和认证诊断仍保留在 AI 设置与连接测试中，便于配置和排障。
- 内置默认提示词改为“日记回顾者”与“回顾口吻”；仅影响新默认值和恢复默认操作，用户已有自定义提示词不迁移。
- 不改变记录、润色、生成、保存和历史只读流程，也不修改服务端、Markdown 或 Parser。

### 阶段 3：清理展示层 emoji

**状态：已实施。**

- 新增展示层标题解析规则，按 `DiarySection.sectionType` 将八种系统章节显示为规范标题：习惯打卡、随手记、小确幸、焦虑时刻、觉察、今日回顾、明日寄语和影像记录。
- `HabitCard`、`GenericSectionCard` 和今日回顾展示统一使用该规则；标题旁不新增统一 Flora 图标，现有操作图标和今日回顾内部小标题图标保持不变。
- `generic` 章节继续原样保留自定义标题，包括用户主动使用的 emoji；空自定义标题仍不渲染标题。
- Markdown 标题、Parser、领域模型和旧格式兼容保持不变，旧习惯项目中的 emoji 仍由 `HabitIcon` 负责兼容显示。

#### 阶段 2、3 验收记录

- `test/widget_test.dart` 覆盖润色文案、错误脱敏、今日回顾状态、默认提示词、自定义提示词保留和系统章节标题映射。
- `flutter analyze`：通过；`flutter test`：454 项通过；`git diff --check`：通过。
- `test_36` Debug 包已通过 `adb install -r` 安装；浅色、深色、1.3 倍字体和约 320dp 窄屏均完成检查，验收后恢复模拟器物理尺寸、浅色模式和 1.0 倍字体。
- 模拟器检查确认快速记录显示「润色」，今日页显示「今日回顾 / 更新回顾」，焦虑与回顾正文正常换行，明日寄语不带背景，影像记录保持两列方形网格；未出现 API、Model、状态码或旧展示标题。

### 阶段 4：统一图标语言

- [x] 修正图标名称与实际资源不匹配的问题，例如 `shuffle` 不再映射到群组图标，图片关闭入口不再使用加号资源。
- [x] 规范步行、语言、跑步、日出、冥想、力量、苹果、书籍、图钉、暖心反馈和随机等 Flora 资源的 24×24 画板、2px 圆角描边和 `currentColor`。
- [x] Material 图标只承担返回、关闭、编辑、恢复、重置、删除、刷新、展开、显隐、方向和播放暂停等通用操作；日记、习惯、回顾和设置分类继续使用 Flora。
- [x] 记录 Tabler Icons `v3.46.0` 的静态资源来源与 MIT 许可，不增加运行时依赖。

### 阶段 5：修正颜色层级与对比度

- [x] 调整浅色模式主色、次级文字、错误文字和功能边界，使普通字号达到 WCAG AA。
- [x] 限制 muted 色仅用于禁用状态和非关键信息。
- [x] 显式定义 `onSurfaceVariant`、`surfaceContainerHighest`、`outline`、`outlineVariant` 和 `surfaceTint` 等 Material 3 语义色。
- [x] 标签文字按透明背景混合后的最终颜色计算对比度；Callout 使用主题感知的蓝、橙、红、绿和紫色组合，并保留图标与文字语义。
- [x] 深色模式同步建立暖黑页面、内容表面、边界、错误和系统导航栏层级。

#### 阶段 4、5 验收记录

- 新增 `test/design_system_test.dart`，覆盖规范化 Flora SVG、逻辑资源映射、浅色/深色 Material 3 语义色和 Today Rainbow 标签最终混合背景对比度。
- 标签对比度同时按内容表面与页面底色计算；Callout 颜色集中到主题扩展；通用操作使用圆角 Material 图标，条目和影像菜单保留 48dp 热区。
- 黑底影像预览使用页面级系统栏样式，不被普通页面纸面色覆盖。
- `flutter test test/design_system_test.dart`、`flutter test test/widget_test.dart`、`flutter test test/open_layout_width_test.dart` 和 `flutter analyze` 均通过。
- `test_36` 已安装最新 Debug 包；浅色首页、设置页、深色首页均完成截图检查，返回、随机、关闭、习惯图标和设置分类图标语义正常。
- 临时切换约 320dp 宽与 1.3 倍字体后，首页习惯行、日期标题、FAB、底部导航、标签菜单和图片预览未出现溢出或遮挡；验收后已恢复 420 密度、1.0 倍字体和浅色模式。本轮未重新构建 Release。

### 阶段 6：让快速记录回到写作本身

**状态：已实施。**

- [x] 将完整的记录时间卡片收敛为编辑器上方的 48dp 轻量可点击元信息行，保留今天、历史日期和时间选择行为。
- [x] 让正文编辑区使用主要可用高度和无外框输入，今日新建自动聚焦，编辑与历史补录不主动弹出键盘。
- [x] 将已选标签摘要移到正文下方、工具栏上方；标签面板改为页面内渐进展开，限制为可用主体高度的 40% 且最多 240dp。
- [x] 简化标签展开按钮为单一入口和一个 Material 展开方向图标；展开或时间选择时主动收起键盘，折叠后不自动恢复焦点。
- [x] 保留保存、润色、标签、草稿、编辑、隐藏标签、错误重试和真实数据写入行为，不修改公开构造参数、Markdown、Parser 或服务端。

#### 阶段 6 验收记录

- 新增 `test/quick_capture_layout_test.dart`，覆盖元信息行、自动聚焦、正文高度、标签摘要与唯一展开图标、键盘焦点和 320dp/1.3 倍字体布局。
- `flutter test test/quick_capture_layout_test.dart`：11 项通过；`flutter test test/widget_test.dart`：416 项通过；`flutter test test/open_layout_width_test.dart`：12 项通过；完整 `flutter test`：500 项通过；`flutter analyze` 和 `git diff --check` 均通过。
- `test_36` 已安装最新 Debug 包并完成浅色、深色、今日新建自动聚焦、标签展开收起、1.3 倍字体和约 320dp 窄屏检查；编辑和历史补录不自动聚焦由独立 Widget 测试覆盖。保存按钮在键盘上方保持可见，标签面板内部滚动且未出现溢出。验收后已恢复物理尺寸、420 密度、1.0 倍字体、浅色模式和默认软键盘设置，未修改真实 Vault。

### 阶段 7：全局细节收尾

- [x] 统一首页日期与一级页面标题层级，二级 AppBar 标题统一为 18sp / w600。
- [x] 收敛圆角到图片 8、内容 12、浮层 16、紧凑操作 pill 四档，保留时间轴、进度条和热力图的几何例外。
- [x] 将温柔文案集中在空状态和反馈状态，删除过往页常驻抒情副标题，将习惯热力图标题改为“最近 30 天”。
- [x] 用内容形状占位或保留旧内容刷新，减少全屏通用圆形加载器；新增统一错误状态和重试入口。
- [x] 修正深色模式启动页亮屏，并尊重系统的减少动态效果设置；统一 `FloraMotion` 动效时长。

#### 阶段 7 验收记录

- 新增 `test/stage7_ui_finish_test.dart`，覆盖标题层级、四档圆角、内容占位、空状态合并、减少动态效果和浅深色启动资源；更新核心 Widget 测试以断言新占位和新文案。
- `flutter test test/stage7_ui_finish_test.dart`、`test/design_system_test.dart`、`test/quick_capture_layout_test.dart`、`test/open_layout_width_test.dart` 和 `test/widget_test.dart` 均通过；`flutter analyze` 与 `git diff --check` 通过。
- 第一批页面已接入首页、过往、习惯趋势、历史详情、快速记录、焦虑和启动流程；第二批当前保留的设置、外观、习惯/标签编辑、API/AI、图片设置、关于、初始设置和专注计时页面完成圆角、占位与动效令牌清理。备份与 WebDAV 页面已按后续产品决定撤回，不属于当前应用范围。
- `test_36` 已完成浅色、深色、1.3 倍字体、约 320dp 窄屏和关闭系统动画检查；冷启动背景与页面背景一致，未修改真实 Vault。

### 全阶段复审修正

**状态：已完成。**

- 修复 `TagPicker` 外部标签和标签配置更新后的残留选择；润色缩减标签后再次选择不会带回旧主题或方法。
- 恢复今日回顾、普通标签、隐藏标签删除入口和图片预览关闭入口的常规 `48dp` 触控热区，并保留紧凑视觉尺寸。
- 为图片缩略图、全屏预览、骨架占位和错误状态补齐按钮、加载和 live region 语义；全屏预览支持明确的关闭按钮和背景关闭。
- 统一 `headlineLarge` 行高、习惯进度与饮水/步数面板的 `FloraMotion`，并集中使用 `TodayRainbow` 展示层令牌。
- 首页、过往画廊和历史详情刷新失败时保留已有内容；新增 `test/stage7_refresh_state_test.dart` 覆盖 pending、成功替换和失败保留。
- `flutter test test/quick_capture_layout_test.dart`、`test/design_system_test.dart`、`test/stage7_ui_finish_test.dart`、`test/stage7_refresh_state_test.dart`、`test/open_layout_width_test.dart`、`test/widget_test.dart`、`flutter analyze` 和 `git diff --check` 均已通过。
- `test_36` 已完成浅色、深色、1.3 倍字体和标签展开的 Debug 验收；首页、设置页、快速记录页无明显溢出或遮挡，验收数据未写入真实 Vault，模拟器设置已恢复。
- 以最新 `main` 为基线复跑后，完整 `flutter test` 520 项通过，`flutter analyze` 无问题；`1.7.5+31` Release APK 已安装并在 `test_36` 启动。备份/WebDAV 相关页面保持撤回状态。

## 5. 执行与验收流程

每个阶段都遵循同一循环：

1. 先确认该阶段的目标、视觉结构和明确不改的内容。
2. 只修改当前阶段涉及的组件，不顺手重构相邻功能。
3. 运行 `flutter analyze` 和 `flutter test`。
4. 在 320dp 窄屏、浅色模式、深色模式和系统字体放大下检查布局。
5. 构建 APK 后，优先在 `test_36` 模拟器截取修改前后同一状态；发布前再补充 PLG110 真机比较。
6. 验收通过后，再把稳定结论写入 `DESIGN.md` 或 `AGENTS.md`。

## 6. 完成定义

计划完成时，荔枝日记应保留原有品牌识别和功能稳定性，同时达到以下结果：

- 用户第一眼看到的是自己的日期、记录和相片，而不是组件样式或 AI 功能。
- 不同领域内容拥有合适的阅读结构，不再只是同款卡片换颜色。
- 图标、文字、颜色、圆角和状态反馈形成稳定一致的系统。
- 暖色与克制不以牺牲对比度、触控清晰度和大字体可读性为代价。
- 所有视觉变化均可通过模拟器截图、交互回归和自动化测试验证，并在发布前补充真机确认。
